// const randomBoolean = () => !!(Math.floor(Math.random() * 10) % 2)

// this.submitTimeout = setTimeout(() => {
//   const randomChance = randomBoolean()

//   switch (randomChance) {
//     case true:
//       notification.success()
//       break
//     case false:
//       notification.error()
//       break
//   }

//   const message = randomChance ? 'success' : 'error'

/* =========== Progressbar =========== */

// this.progress = 10
// state.gennySubmitting = true
// setTimeout(() => {
//   state.gennyUploadProgress = this.progress * 1.5
// }, this.progress * 100)
// setTimeout(() => {
//   state.gennyUploadProgress = this.progress * 3
// }, this.progress * 150)
// setTimeout(() => {
//   state.gennyUploadProgress = this.progress * 4
// }, this.progress * 200)
// setTimeout(() => {
//   state.gennyUploadProgress = this.progress * 6
// }, this.progress * 300)
// setTimeout(() => {
//   state.gennyUploadProgress = this.progress * 9
// }, this.progress * 450)
// setTimeout(() => {
//   state.gennyUploadProgress = 0
//   state.gennySubmitting = false
// }, this.progress * 500)
